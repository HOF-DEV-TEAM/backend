image: atlassian/default-image:3

pipelines:
  default:
    - parallel:
        - step:
            name: Build and publish docker image
            script:
              - IMAGE_NAME=$BITBUCKET_REPO_SLUG
              - docker build -t ${APP_NAME} -f Dockerfile .
              - docker login --username=_ --password=$HEROKU_TOKEN registry.heroku.com
              - docker tag $APP_NAME registry.heroku.com/$APP_NAME/web
              - docker push registry.heroku.com/$APP_NAME/web
              - docker inspect registry.heroku.com/$APP_NAME/web --format={{.Id}} > WEB_DOCKER_IMAGE_ID
            services:
              - docker
            caches:
              - docker
            artifacts:
              - WEB_DOCKER_IMAGE_ID
        - step:
            name: deploy-to-heroku
            deployment: staging
            script:
              - export WEB_DOCKER_IMAGE_ID=$(cat WEB_DOCKER_IMAGE_ID)
              - chmod +x ./deploy.sh
              - ./deploy.sh
              - echo "Done..."
      
  # branches:
  #   master:
  #     - step:
  #         name: Build and Test
  #         script:
  #           - IMAGE_NAME=$BITBUCKET_REPO_SLUG
  #           - docker build -t ${IMAGE_NAME}:multistage -f Dockerfile.multistage .
  #           - docker save ${IMAGE_NAME} --output "${IMAGE_NAME}.tar"
  #         services:
  #           - docker
  #         caches:
  #           - docker
  #         artifacts:
  #           - "*.tar"
  #     - step:
  #         name: Deploy to Production
  #         deployment: Production
  #         script:
  #           - echo ${DOCKERHUB_PASSWORD} | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
  #           - IMAGE_NAME=$BITBUCKET_REPO_SLUG
  #           - docker load --input "${IMAGE_NAME}.tar"
  #           - VERSION="prod-0.1.${BITBUCKET_BUILD_NUMBER}"
  #           - IMAGE=${DOCKERHUB_NAMESPACE}/${IMAGE_NAME}
  #           - docker tag "${IMAGE_NAME}" "${IMAGE}:${VERSION}"
  #           - docker push "${IMAGE}:${VERSION}"
  #         services:
  #           - docker

options:
  docker: true
# TODO Add another step for heroku deployment